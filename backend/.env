# Use the Internal Database URL from Render (include port). Replace values only if you need to.
DATABASE_URL=postgresql://portfolio_db_8c1j_user:LcgegQDkPEoLnmB9XNuDbbp3XM7mksi7@dpg-d47g31jipnbc73crkckg-a:5432/portfolio_db_8c1j

# Optional: tell the code to use SSL mode (useful for external DB). For internal Render DB this can remain, it won't hurt.
PGSSLMODE=require

# Server settings
PORT=3000
HOST=0.0.0.0

# Frontend site (CORS)
FRONTEND_URL=https://yaswanthneela.me

NODE_ENV=production