# =============================================================================
# Portfolio Backend - Environment Configuration
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file and rename it to .env
# 2. Update the values according to your setup
# 3. NEVER commit .env to version control (it should be in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Format: postgresql://username:password@host:port/database_name
# 
# Local PostgreSQL Example:
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/portfolio_db
#
# Remote PostgreSQL Example (Heroku, Railway, etc.):
# DATABASE_URL=postgresql://user:pass@host.example.com:5432/dbname
#
# With SSL (for cloud databases):
# DATABASE_URL=postgresql://user:pass@host.example.com:5432/dbname?sslmode=require
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/portfolio_db

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port number for the backend server
# Default: 3000
# -----------------------------------------------------------------------------
PORT=3000

# -----------------------------------------------------------------------------
# Frontend URL for CORS (Optional)
# -----------------------------------------------------------------------------
# Your frontend URL that will make requests to this API
# 
# For development:
# FRONTEND_URL=http://localhost:5500
# 
# For production:
# FRONTEND_URL=https://yaswanthneela.me
#
# To allow all origins (NOT RECOMMENDED for production):
# FRONTEND_URL=*
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost:5500

# -----------------------------------------------------------------------------
# Environment Mode (Optional)
# -----------------------------------------------------------------------------
# Set to 'development' for development mode
# Set to 'production' for production mode
# 
# In development mode, all localhost origins are allowed for CORS
# Default: development (if not set)
# -----------------------------------------------------------------------------
NODE_ENV=development

# =============================================================================
# Setup Instructions:
# =============================================================================
# 1. Copy this file: Copy-Item env.template .env  (PowerShell)
#    Or manually create .env and copy the content above
#
# 2. Update DATABASE_URL with your PostgreSQL credentials:
#    - Replace 'your_password' with your actual PostgreSQL password
#    - Replace 'portfolio_db' with your database name (or create it first)
#    - Update host/port if different from default (localhost:5432)
#
# 3. Update FRONTEND_URL to match your frontend URL
#
# 4. Make sure PostgreSQL is running before starting the server
#
# 5. Run 'npm start' to start the server
#    The server will automatically create database tables on startup
#
# 6. To check database connection: .\check-db.ps1 (PowerShell)
# =============================================================================

